# A repo for Cafeine databases docker files 

In order to simplify development on cafeine, here are a few docker files that represent databases.

To launch databases just run :
  * `docker-compose -f cafeine-dev.yml up -d`
  * `docker-compose -f cafeine-test.yml up -d`